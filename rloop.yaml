# rloop, gating itself.
#
# Until this file existed the tool had no verdict on its own releases: `rloop
# gate` had nothing to run, and the only alternative was typing
# `npm test && npx tsc` by hand — the exact false green rloop exists to
# prevent, and circular besides, since it would use an unproven build as
# evidence for shipping that build.
#
# Every marker below was captured from real output, not guessed. Where a
# command prints nothing on success, that is stated rather than papered over.

version: 1

forge:
  provider: github
  slug: tezer/rloop

committer:
  name: tezer
  email: 4751652+tezer@users.noreply.github.com

gates:
  # ── 1. The suite ────────────────────────────────────────────────────────
  - name: test
    run: npx vitest run
    require:
      # A FLOOR of 247, not a bare `\d+ passed`.
      #
      # The failure this guards is a suite that SHRINKS: a renamed directory,
      # a broken import in a conftest-equivalent, a glob that stops matching.
      # vitest exits 0 and prints a cheerful `Tests  50 passed (50)`, which a
      # bare pattern accepts. Measured 2026-08-30 at 247.
      #
      # Anchored at line start with no `failed` alternative on purpose: a red
      # run prints `Tests  1 failed | 217 passed (218)`, which begins with the
      # failure count, so this cannot match it even though "passed" appears.
      #
      # ⚠️ RAISE THIS when the suite grows — nothing will remind you. It only
      # catches shrinkage. Lowering it to make a red run green defeats the point.
      - "^\\s+Tests\\s+(24[7-9]|2[5-9][0-9]|[3-9][0-9]{2}|[0-9]{4,}) passed \\(\\d+\\)$"
      # The file-level twin. A test FILE that fails to load reports
      # `Test Files  1 failed | 20 passed (21)` while individual tests may
      # still tally clean, so the Tests line alone can miss a collection error.
      - "^\\s+Test Files\\s+\\d+ passed \\(\\d+\\)$"
    forbid:
      - "^\\s*FAIL\\s"                    # per-file failure line
      - "Failed Tests"                    # the `⎯⎯ Failed Tests N ⎯⎯` banner
      - "^\\s+Tests\\s+\\d+ failed"       # summary line, red form
      - "^No test files found"            # collected nothing; exit code alone lies
    timeout_seconds: 600

  # ── 2. Both typecheck configs ───────────────────────────────────────────
  - name: typecheck
    # `tsconfig.json` covers src/**; `tsconfig.test.json` covers src + test.
    # The second exists because test/** was never typechecked until this
    # release, and turning it on surfaced three real pre-existing errors.
    #
    # The trailing `echo` is the positive marker: tsc prints NOTHING on
    # success, so without it this gate could only ever assert the absence of
    # failure strings. Be precise about what the marker proves — it proves
    # both tsc invocations exited 0, because `&&` short-circuits. It does not
    # inspect their output. The `forbid` below is what reads that.
    run: npx tsc -p tsconfig.json && npx tsc -p tsconfig.test.json && echo "RLOOP_TYPECHECK_OK src+test"
    require:
      - "^RLOOP_TYPECHECK_OK src\\+test$"
    forbid:
      - "error TS[0-9]+"
    timeout_seconds: 300

  # ── 3. The built artifact actually runs ─────────────────────────────────
  - name: smoke
    # `typecheck` proves the sources compile. `test` already runs the built
    # MCP server (test/bin-symlink.test.ts spawns dist/mcp/server.js through a
    # node_modules/.bin-style symlink and speaks stdio MCP to it) — that is
    # also "what ships", exercised before this gate ever runs. What this gate
    # adds is the OTHER shipped entry point: dist/cli.js, invoked end to end
    # against a real config file on disk. It caught nothing during
    # development — it exists for the release where `dist/` is stale or the
    # CLI entry point is broken, which no unit test sees.
    #
    # `check` alone only proves config PARSING — it never calls `runGates`,
    # the function `gate`, `pr status` and `pr merge` all depend on for a
    # merge verdict. A break confined to `runGates` would sail through
    # `check` unnoticed. The block below closes that gap: it builds a
    # disposable one-gate config inside its own throwaway git repo (never
    # this repo's rloop.yaml — pointing `gate` at that from inside a gate run
    # would recurse) and runs `gate --only probe` against it, so this gate
    # also proves the real `gate` command path — preflight, runGates,
    # evidence evaluation, exit code — actually works end to end.
    run: |
      set -euo pipefail
      npm run build
      node dist/cli.js check -c examples/next-vitest.yaml
      SMOKE_DIR=$(mktemp -d)
      trap 'rm -rf "$SMOKE_DIR"' EXIT
      # Isolate the throwaway repo from whatever is in the developer's
      # gitconfig. Observed: `commit.gpgsign = true` with a signing agent that
      # is not running fails `git commit` with "failed to write commit object",
      # so this gate went red on a machine where nothing was wrong with rloop.
      # A missing config file reads as empty config, which is the intent.
      #
      # Note the direction. src/git.ts UNSETS these so a leaked value cannot
      # redirect rloop's own reads; here they are SET, so a fixture cannot
      # inherit the operator's. Setting them inside the script is what makes
      # that possible — the scrub applies when rloop spawns this gate, not to
      # what the gate exports afterwards.
      export GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null
      git init -q "$SMOKE_DIR"
      git -C "$SMOKE_DIR" config user.email smoke@rloop.invalid
      git -C "$SMOKE_DIR" config user.name "rloop smoke"
      echo seed > "$SMOKE_DIR/seed.txt"
      git -C "$SMOKE_DIR" add seed.txt
      git -C "$SMOKE_DIR" commit -q -m seed
      cat > "$SMOKE_DIR/rloop.yaml" <<'SMOKE_CFG'
      version: 1
      gates:
        - name: probe
          run: echo RLOOP_SMOKE_PROBE_OK
          require:
            - "RLOOP_SMOKE_PROBE_OK"
      SMOKE_CFG
      node dist/cli.js gate -c "$SMOKE_DIR/rloop.yaml" --only probe
    require:
      - "^config OK: .*examples/next-vitest\\.yaml$"
      - "PARTIAL — 1 selected gate\\(s\\) passed"
    forbid:
      - "invalid config"
      - "error TS[0-9]+"
      - "^rloop: "
    timeout_seconds: 300

# ── Who must review ───────────────────────────────────────────────────────
reviewers:
  - name: copilot
    kind: forge
    login: copilot-pull-request-reviewer
    required_state: any_verdict
    # `any_verdict`, not `approved`, and the reason matters: Copilot never
    # submits APPROVED at all — it files findings as inline comments on a
    # COMMENTED review. Under `approved` it would block this repo forever.
    #
    # Understand what that leans on. `any_verdict` means "the reviewer turned
    # up", and the real gate on its findings is require_threads_resolved
    # below. Thread resolution is performed by the same agent being gated, so
    # this delegates part of the check to a party with an interest in the
    # answer. That is a known, accepted weakness, not an oversight.

  # A local second opinion, dogfooding this release's own feature. Commented
  # out because it needs a `codex` binary on PATH; uncomment once you have
  # one. It must print ONE JSON document on stdout and narrate on stderr.
  #
  # `inject_sha` is what 0.4.0 added: rloop supplies the sha rather than asking
  # a model to copy a 40-character hex string into a JSON field. Sound for this
  # provider because it reviews committed state; see examples/reviewers/README.md
  # for the four other safety properties, which the SCRIPT still owns.
  # - name: codex
  #   kind: command
  #   run: ./examples/reviewers/codex-review.sh
  #   timeout_seconds: 900
  #   inject_sha: true

merge:
  # ENABLED. The loop closes by itself when every condition below holds.
  #
  # Know precisely what this does and does not authorise. It merges into
  # `main`; it does NOT publish. `npm publish` needs a credential this tool
  # does not have and cannot use, so shipping stays a deliberate human act.
  # A merge here is revertable; a publish is not, and that asymmetry is the
  # reason the two are separated rather than chained.
  #
  # What still stands between a generated PR and `main`: all three gates
  # green ON THE HEAD COMMIT, a reviewer verdict bound to that same commit,
  # and every review thread resolved. `pr merge` re-derives all of it rather
  # than trusting a verdict computed moments earlier — a push between the
  # check and the merge invalidates the check, and it will refuse.
  enabled: true

  # ALLOWLIST, never a denylist — a branch nobody remembered to add stays
  # protected. Populated so that flipping `enabled` above is a one-line change
  # rather than two, and so the allowlist is reviewed now rather than in a hurry.
  allowed_base_branches: [main]

  method: squash
  delete_branch: true

  # The real gate on a comment-only reviewer's findings. See the note under
  # `required_state` above for what this is carrying.
  require_threads_resolved: true

  # `reviewer_timeout_seconds: 900` used to sit here. It was inert — parsed,
  # validated, and read by nothing, because the forge-verdict polling it was
  # meant to bound was never built. `rloop check` now warns about it, so leaving
  # it would mean rloop's own config trips rloop's own warning.
